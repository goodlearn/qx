<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.SpotCheckSinglePersonDao">
    
	<sql id="spotCheckSinglePersonColumns">
		a.id AS "id",
		a.scmp_id AS "scmpId",
		a.work_person_id AS "workPersonId",
		a.submit_state AS "submitState",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="spotCheckSinglePersonJoins">
	</sql>
    
	<select id="get" resultType="SpotCheckSinglePerson">
		SELECT 
			<include refid="spotCheckSinglePersonColumns"/>
		FROM spot_check_single_person a
		<include refid="spotCheckSinglePersonJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SpotCheckSinglePerson">
		SELECT 
			<include refid="spotCheckSinglePersonColumns"/>
		FROM spot_check_single_person a
		<include refid="spotCheckSinglePersonJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="scmpId != null and scmpId != ''">
				AND a.scmp_id = #{scmpId}
			</if>
			<if test="workPersonId != null and workPersonId != ''">
				AND a.work_person_id = #{workPersonId}
			</if>
			<if test="submitState != null and submitState != ''">
				AND a.submit_state = #{submitState}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SpotCheckSinglePerson">
		SELECT 
			<include refid="spotCheckSinglePersonColumns"/>
		FROM spot_check_single_person a
		<include refid="spotCheckSinglePersonJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO spot_check_single_person(
			id,
			scmp_id,
			work_person_id,
			submit_state,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{scmpId},
			#{workPersonId},
			#{submitState},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE spot_check_single_person SET 	
			scmp_id = #{scmpId},
			work_person_id = #{workPersonId},
			submit_state = #{submitState},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE spot_check_single_person SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>