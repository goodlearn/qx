<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.SpotCheckMainPersonDao">
    
	<sql id="spotCheckMainPersonColumns">
		a.id AS "id",
		a.ws_mask_wc_id AS "wsMaskWcId",
		a.work_person_id AS "workPersonId",
		a.run_time AS "runTime",
		a.submit_state AS "submitState",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="spotCheckMainPersonJoins">
	</sql>
    
	<select id="get" resultType="SpotCheckMainPerson">
		SELECT 
			<include refid="spotCheckMainPersonColumns"/>
		FROM spot_check_main_person a
		<include refid="spotCheckMainPersonJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SpotCheckMainPerson">
		SELECT 
			<include refid="spotCheckMainPersonColumns"/>
		FROM spot_check_main_person a
		<include refid="spotCheckMainPersonJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="wsMaskWcId != null and wsMaskWcId != ''">
				AND a.ws_mask_wc_id = #{wsMaskWcId}
			</if>
			<if test="workPersonId != null and workPersonId != ''">
				AND a.work_person_id = #{workPersonId}
			</if>
			<if test="runTime != null and runTime != ''">
				AND a.run_time = #{runTime}
			</if>
			<if test="submitState != null and submitState != ''">
				AND a.submit_state = #{submitState}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SpotCheckMainPerson">
		SELECT 
			<include refid="spotCheckMainPersonColumns"/>
		FROM spot_check_main_person a
		<include refid="spotCheckMainPersonJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO spot_check_main_person(
			id,
			ws_mask_wc_id,
			work_person_id,
			run_time,
			submit_state,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{wsMaskWcId},
			#{workPersonId},
			#{runTime},
			#{submitState},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE spot_check_main_person SET 	
			ws_mask_wc_id = #{wsMaskWcId},
			work_person_id = #{workPersonId},
			run_time = #{runTime},
			submit_state = #{submitState},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE spot_check_main_person SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>